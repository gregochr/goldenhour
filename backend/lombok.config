# Stop Lombok searching for parent config files
config.stopBubbling = true

# Add @lombok.Generated to all Lombok-generated code.
# JaCoCo 0.8+ automatically excludes @Generated methods from coverage analysis,
# so coverage metrics reflect only handwritten business logic.
lombok.addLombokGeneratedAnnotation = true
