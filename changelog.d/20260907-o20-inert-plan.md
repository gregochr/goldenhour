### Changed — O-20's cure is costed, and it cannot be protected by CI

`map-tab-v2-plan.md` O-20 names "shell-root `inert` while any dialog is open" as its cure in one
line. `docs/engineering/o20-shell-inert-plan.md` costs it, and the costing turned up something
neither that record nor `useDialogFocus`'s ruling states: **`inert` is a silent no-op in this
project's jsdom** — measured, `focus reached a button inside an inert div: true` — so a test
asserting the guard works passes whether it exists or not. Playwright would see it and does not run
in CI. Three remaining arms are enumerated, the ruling's two unstarted prerequisites are verified as
real, and the decision is put to the owner rather than assumed.
